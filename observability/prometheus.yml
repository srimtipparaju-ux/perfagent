global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'perfagent'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['perfagent:3000']
        labels:
          service: 'perfagent'
          env:     'production'
